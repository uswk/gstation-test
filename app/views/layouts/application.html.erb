<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Station" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="turbo-visit-control" content="reload" %>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%# Includes all stylesheet files in app/assets/stylesheets %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= yield(:js) %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body class="p-2" turbo="false" turbo="false" data-turbo-prefetch="false" <%= yield(:body_attributes) if content_for?(:body_attributes) %>>
 <p class="alert alert-success" role="alert" id="notice"><%= notice %></p>
 <p class="alert alert-danger" role="alert" id="alert"><%= alert %></p>

<% if user_signed_in? && (current_user.login_authority == 1 || current_user.authority==1) && @header_no_dsp.nil? %>
  <%= render 'shared/header' %>
<% end %>
<%= render 'shared/common' %>
<% if user_signed_in? %>
  <% if current_user.login_authority == 1 || current_user.authority==1 %>
    <%= yield %>
    <%= yield :scripts %>
  <% else %>
    <font color=red>ログイン権限がありません。</font><br>
    <font color=red>管理者に連絡してください。</font><br><br>
    <%= link_to "ログインページへ", destroy_user_session_path, :method => :get %>
  <% end %>
<% else %>
  <%= yield %>
  <%= yield :scripts %>
<% end %>
</body>
</html>
